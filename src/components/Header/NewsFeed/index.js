import React from 'react';
import { LabelInfo } from '../styled';

class NewsFeed extends React.PureComponent {
  render() {
    return (
      <LabelInfo>Лента</LabelInfo>
    );
  }
}

export default NewsFeed;