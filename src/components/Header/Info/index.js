import React from 'react';
import { LabelInfo } from '../styled';

class Info extends React.PureComponent {
  render() {
    return (
      <LabelInfo>3 место</LabelInfo>
    );
  }
}

export default Info;