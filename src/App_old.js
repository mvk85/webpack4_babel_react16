import React, { Component } from "react";
import ReactDOM from "react-dom";

const App_old = () => {
  return (
    <div>
      <p>React here!</p>
    </div>
  );
};

export default App_old;

ReactDOM.render(<App_old />, document.getElementById("app"));
